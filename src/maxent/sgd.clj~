(in-ns 'maxent.core)
;;; sequential conditional iterative scaling.


(defn train-sgd
  "trains a maxent model using scis"
  [coll]
  )

(defn iter-sgd
  "iteration of scis"
  [coll]
  (let [observed]))
